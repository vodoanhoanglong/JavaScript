// Viết hàm đảo ngược chuỗi
// Example
// reverse('abc') // 'cba'
function reverse(str) {
    // viết code ở đây.
    var string = str.split("");
    var arrStr = string.reverse();
    return arrStr.join("");
  }