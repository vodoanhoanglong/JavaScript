const name = ['Long1', 'Long2', 'Long3'];
for(let i=0; i<name.length; i++)
{
    console.log(name[i]);
}

const obj = {
    0: 'Long4',
    1: 'Long5',
    2: 'Long6',
    length: 3
}

for(let i=0; i<obj.length; i++)
{
    console.log(obj[i]);
}